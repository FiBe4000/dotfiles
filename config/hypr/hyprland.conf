# This is an example Hyprland config file.
# Syntax is the same as in Hypr, but settings might differ.
#
# Refer to the wiki for more information.

monitor=,preferred,auto,1
workspace=DP-1,1

input {
    kb_layout=us,se
    kb_variant=
    kb_model=
    kb_options=grp:win_space_toggle,caps:escape
    kb_rules=

    follow_mouse=1
}

general {
    sensitivity=3.0 # for mouse cursor
    cursor_inactive_timeout=0

    gaps_in=6
    gaps_out=0,12,12,12
    border_size=3
    col.active_border=0xff81a1c1
    col.inactive_border=0xff4c566a

    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)

#damage_tracking=full # leave it on full unless you hate your GPU and want to make it suffer
}

decoration {
    rounding=13
    blur {
        enabled=true
        size=3 # minimum 1
        passes=3 # minimum 1, more passes = more resource intensive.
        new_optimizations=true
    }
    # Your blur "amount" is blur_size * blur_passes, but high blur_size (over around 5-ish) will produce artifacts.
    # if you want heavy blur, you need to up the blur_passes.
    # the more passes, the more you can up the blur_size without noticing artifacts.
}

animations {
    enabled=1
    animation=windows,1,3,default
    animation=border,1,10,default
    animation=fadeIn,1,10,default
    animation=workspaces,1,1,default
}

gestures {
    workspace_swipe=true
    workspace_swipe_fingers=3
}

dwindle {
    pseudotile=0 # enable pseudotiling on dwindle
}

misc {
    disable_hyprland_logo=true
    disable_splash_rendering=true
}
# example window rules
# for windows named/classed as abc and xyz
#windowrule=move 69 420,abc
#windowrule=size 420 69,abc
#windowrule=tile,xyz
#windowrule=float,abc
#windowrule=pseudo,abc
#windowrule=monitor 0,xyz
windowrulev2 = noblur, title:^()$, class:^()$

layerrule = blur, rofi
layerrule = animation fadeIn, rofi
layerrule = blur, notifications

bind=SUPERSHIFT,Q,exit,
bind=SUPERCONTROLSHIFT,Q,exec,poweroff
bind=SUPERCONTROLSHIFT,R,exec,reboot
bind=CONTROLALT,L,exec,hyprlock
bind=SUPERSHIFTENTER,RETURN,exec,kitty
bind=SUPER,C,killactive,
bind=SUPER,E,exec,dolphin
bind=SUPER,S,togglefloating,
bind=SUPER,F,fullscreen,0
bind=SUPER,M,fullscreen,1
bind=SUPER,P,exec,rofi -show drun

bind=SUPER,H,movefocus,l
bind=SUPER,L,movefocus,r
bind=SUPER,K,movefocus,u
bind=SUPER,J,movefocus,d

bind=SUPERSHIFT,H,movewindow,l
bind=SUPERSHIFT,L,movewindow,r
bind=SUPERSHIFT,K,movewindow,u
bind=SUPERSHIFT,J,movewindow,d

bindm=SUPER,mouse:272,movewindow
bindm=SUPER,mouse:273,resizewindow

bind=SUPER,1,moveworkspacetomonitor,1 current
bind=SUPER,1,workspace,1
bind=SUPER,2,moveworkspacetomonitor,2 current
bind=SUPER,2,workspace,2
bind=SUPER,3,moveworkspacetomonitor,3 current
bind=SUPER,3,workspace,3
bind=SUPER,4,moveworkspacetomonitor,4 current
bind=SUPER,4,workspace,4
bind=SUPER,5,moveworkspacetomonitor,5 current
bind=SUPER,5,workspace,5
bind=SUPER,6,moveworkspacetomonitor,6 current
bind=SUPER,6,workspace,6
bind=SUPER,7,moveworkspacetomonitor,7 current
bind=SUPER,7,workspace,7
bind=SUPER,8,moveworkspacetomonitor,8 current
bind=SUPER,8,workspace,8
bind=SUPER,9,moveworkspacetomonitor,9 current
bind=SUPER,9,workspace,9
bind=SUPER,0,moveworkspacetomonitor,10 current
bind=SUPER,0,workspace,10

bind=SUPERSHIFTENTER,1,movetoworkspace,1
bind=SUPERSHIFTENTER,2,movetoworkspace,2
bind=SUPERSHIFTENTER,3,movetoworkspace,3
bind=SUPERSHIFTENTER,4,movetoworkspace,4
bind=SUPERSHIFTENTER,5,movetoworkspace,5
bind=SUPERSHIFTENTER,6,movetoworkspace,6
bind=SUPERSHIFTENTER,7,movetoworkspace,7
bind=SUPERSHIFTENTER,8,movetoworkspace,8
bind=SUPERSHIFTENTER,9,movetoworkspace,9
bind=SUPERSHIFTENTER,0,movetoworkspace,10

bind=,XF86MonBrightnessDown,exec,xbacklight -dec 10 -time 200 -steps 10
bind=,XF86MonBrightnessUp,exec,xbacklight -inc 10 -time 200 -steps 10
bind=SHIFT,XF86MonBrightnessDown,exec,xbacklight -dec 5 -time 200 -steps 10
bind=SHIFT,XF86MonBrightnessUp,exec,xbacklight -inc 5 -time 200 -steps 10

# Bind switches
#bindl=,switch:Lid Switch,exec,hyprlock
#bindl=,switch:on:Lid Switch,exec,hyprctl keyword monitor "eDP-1, disable"
#bindl=,switch:off:Lid Switch,exec,hyprctl keyword monitor "eDP-1, 2560x1440, 0x0, 1"

exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once=systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once=~/.dotfiles/scripts/launchportals.sh
exec-once=~/.dotfiles/scripts/islaptop.sh

exec-once=swaybg -i $HOME/Pictures/wallpaper/17.jpg -m fill
exec-once=dunst
exec-once=nm-applet
exec-once=gammastep-indicator
exec-once=headset-charge-indicator --headsetcontrol-binary /usr/bin/headsetcontrol
exec-once=blueman-applet
exec-once=/usr/lib/geoclue-2.0/demos/agent
exec-once=udiskie
exec-once=pactl load-module module-switch-on-connect
exec-once=playerctl status --format '{{ playerName }}' --follow > /tmp/player-last
exec-once=hypridle
exec-once=eww open bar
